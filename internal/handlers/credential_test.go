package handlers

import (
	"testing"
)

// TestHandlersCredentialCreate tests the credential creation endpoint.
func TestHandlersCredentialCreate(t *testing.T) {
	t.Skip("skipping handler tests; requires running server and database")
}

// TestHandlersCredentialList tests the credential listing endpoint.
func TestHandlersCredentialList(t *testing.T) {
	t.Skip("skipping handler tests; requires running server and database")
}

// TestHandlersPublicVerify tests the public verification endpoint.
func TestHandlersPublicVerify(t *testing.T) {
	t.Skip("skipping handler tests; requires running server and database")
}